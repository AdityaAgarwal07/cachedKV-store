#!/usr/bin/env bash
set -eu
echo "Run the server for each policy and invoke BenchmarkClient; results are intentionally not fabricated."
echo "Example: java -jar target/redis-lite-1.0-SNAPSHOT.jar --aof-fsync always"
